# Code quality checks workflow - intentionally DISABLED
# TODO: Re-enable this workflow (uncomment `name:` and update triggers)
#       once the code quality pipeline is ready and approved.

# name: Code Quality

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  # Placeholder job - workflow disabled
  placeholder:
    name: Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Code quality checks disabled"
