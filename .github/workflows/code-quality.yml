# Code quality checks - DISABLED for now
# Manual trigger only

name: Code Quality

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  # Placeholder job - workflow disabled
  placeholder:
    name: Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Code quality checks disabled"
